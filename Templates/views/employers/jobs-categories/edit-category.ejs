<%- include('../partials/main_header') %>
<%- include('../partials/navebar_header') %>
<%- include('../partials/side_bar') %>
<!-- @extends('employer.main')
@section('content') -->
    <style>
        li {
            font-weight: 600;
            list-style: none;
            color: red;
        }
    </style>
    <div class="content-body" style="min-height: 788px;">
        <!-- row -->
        <div class="container-fluid">
            <div class="d-flex align-items-center mb-4">
                <h4 class="fs-20 font-w600 mb-0 me-auto">Update Category</h4>
            </div>
            <div class="row">
                <div class="col-xl-6 m-auto">
<!-- 
                    @if (session('error'))
                        <div class="alert alert-info alert-alt solid fade show" role="alert">
                            {{ session('error') }}
                        </div>
                    @endif -->

                    <div class="card">
                        <div class="card-body">
                            <form method="POST" action="" enctype="multipart/form-data">
                                <!-- @csrf -->
                                <!-- <input type="hidden" name="category_id"  value="{{ $category->id }}"> -->
                                <div class="row">
                                    <div class="col-xl-12  col-md-12 mb-4">
                                        <label class="form-label font-w600">Category Name<span class="text-danger scale5 ms-2">*</span></label>
                                        <input type="text" name="category_name" class="form-control solid @error('category_name') is-invalid @enderror" value="<%=categoryName%>" placeholder="Category Name">
                                        <x-input-error :messages="$errors->get('category_name')" class="mt-1" />
                                    </div>
                                    <div class="col-xl-12  col-md-12 mb-4">
                                        <label class="form-label font-w600">Category Image<span class="text-danger scale5 ms-2">*</span></label>
                                        <input type="file"  name="category_image" class="form-control solid  @error('category_image') is-invalid @enderror">
                                        <x-input-error :messages="$errors->get('category_image')" class="mt-1" />
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-lg-4 col-md-6">
                                        <label class="form-label font-w600">Previous Image</label>
                                        <img class="rounded-circle img-fluid" src="/db_assets/p-images/<%=categoryImage%>" alt="" width="100">
                                    </div>
                                </div>
                                <div class="card-footer text-end">
                                    <div>
                                        <button type="submit" class="btn btn-secondary">Submit</button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

<%- include('../partials/footer') %>  
