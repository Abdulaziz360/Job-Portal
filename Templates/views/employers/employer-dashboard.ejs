<%- include('partials/main_header') %>
<%- include('partials/navebar_header') %>
<%- include('partials/side_bar') %>

    <!--**********************************
            Content body start
        ***********************************-->
    <div class="content-body">
        <!-- row -->
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-12 col-md-12 col-12">
                    <div class="row">
                        <div class="col-xl-12">
                            <div class="card">
                                <div class="card-body">
                                    <div class="row separate-row">
                                        <div class="col-sm-6">
                                            <div class="job-icon pb-4 d-flex justify-content-between">
                                                <div>
                                                    <div class="d-flex align-items-center mb-1">
                                                        <h2 class="mb-0">
                                                            <!-- @php  $total_jobs = DB::table('jobs')->where('emp_id',Auth::user()->id)->count(); @endphp -->
                                                             <%=total_jobs%>
                                                        </h2>
                                                    </div>
                                                    <span class="fs-14 d-block mb-2">Total Posted Jobs</span>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="col-sm-6">
                                            <div class="job-icon pb-4 d-flex justify-content-between">
                                                <div>
                                                    <div class="d-flex align-items-center mb-1">
                                                        <h2 class="mb-0">
                                                         <%=active_jobs%>
                                                        </h2>
                                                    </div>
                                                    <span class="fs-14 d-block mb-2">Total Active Jobs</span>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="col-sm-6">
                                            <div class="job-icon pb-4 pt-4 pt-sm-0 d-flex justify-content-between">
                                                <div>
                                                    <div class="d-flex align-items-center mb-1">
                                                        <h2 class="mb-0">
                                                            <!-- @php  $total_app_jobs = DB::table('candidate_applied_jobs')->where('emp_id',Auth::user()->id)->count(); @endphp -->
                                                            <%=applied_jobs%>
                                                        </h2>
                                                    </div>
                                                    <span class="fs-14 d-block mb-2">Total Applied Jobs </span>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-sm-6">
                                            <div class="job-icon pt-4 pb-sm-0 pb-4 pe-3 d-flex justify-content-between">
                                                <div>
                                                    <div class="d-flex align-items-center mb-1">
                                                        <h2 class="mb-0">
                                                            <!-- @php  $total_saved_jobs = DB::table('candidate_saved_jobs')->where('emp_id',Auth::user()->id)->count(); @endphp -->
                                                            <%=saved_jobs%>
                                                        </h2>
                                                    </div>
                                                    <span class="fs-14 d-block mb-2">Total Saved Jobs</span>
                                                </div>
                                            </div>
                                        </div>

                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--**********************************
        Content body end
    ***********************************-->

    <%- include('partials/footer') %>



    <script>
        function JobickCarousel()
        {

            /*  testimonial one function by = owl.carousel.js */
            jQuery('.front-view-slider').owlCarousel({
                loop:false,
                margin:30,
                nav:true,
                autoplaySpeed: 3000,
                navSpeed: 3000,
                autoWidth:true,
                paginationSpeed: 3000,
                slideSpeed: 3000,
                smartSpeed: 3000,
                autoplay: false,
                animateOut: 'fadeOut',
                dots:true,
                navText: ['', ''],
                responsive:{
                    0:{
                        items:1
                    },

                    480:{
                        items:1
                    },

                    767:{
                        items:3
                    },
                    1750:{
                        items:3
                    }
                }
            })
        }

        jQuery(window).on('load',function(){
            setTimeout(function(){
                JobickCarousel();
            }, 1000);
        });
    </script>


